{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|Bash|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(echo \"$TOOL_INPUT\" | ruby -rjson -e 'puts JSON.parse(STDIN.read).values_at(\"file_path\",\"path\").compact.first rescue nil' 2>/dev/null); ruby bin/harness gate before-tool --tool \"$TOOL_NAME\" ${FILE_PATH:+--file-path \"$FILE_PATH\"}"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "ruby bin/harness state update --tool \"$TOOL_NAME\" --exit-code \"$EXIT_CODE\""
          }
        ]
      }
    ]
  }
}
